# Architecture

Event -> Risk Detector -> Root Cause Agent -> Intervention Agent -> Recovery Executor -> Outcome Analyzer.

The AI layer recommends decisions, while integrations execute bounded actions.
